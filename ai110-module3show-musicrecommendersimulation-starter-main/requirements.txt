# Core (required) — the RAG feature runs fully offline on the standard library.
pytest

# Original Module 1-3 project deps (kept for compatibility).
pandas
streamlit

# Optional — only needed to turn ON the Gemini LLM generation path.
# Without this (or without GEMINI_API_KEY) the app uses the deterministic
# offline generator automatically.
# google-genai
